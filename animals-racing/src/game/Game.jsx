import React, { useEffect, useState } from 'react';
import { Stage, Layer, Star, Text, Circle, Rect, Line } from 'react-konva';

export const Game = ({ }) => {

    useEffect(() => {

    }, [])

    return (
        <></>
    );
};
